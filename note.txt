services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: qwerty

  mongo-express:
    image: mongo-express
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: qwerty
      ME_CONFIG_MONGODB_URL: mongodb://admin:qwerty@mongo:27017/

  testapp:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo



✅ How to run (ONLY this way)
docker compose up --build


Then open:

App → http://localhost:3000

Mongo Express → http://localhost:8081

🧪 Why your old approach partially worked
Scenario	Result
Node locally + Mongo container	✅ works
Node container + Mongo container	❌ fails
All services in compose	✅ works